import XCTest

// Original tests targeted the legacy MiniLM EmbeddingInference + EmbeddingModel
// path with a constructor signature that no longer exists post-Gemma sunset.
// Stubbed empty so the test target keeps compiling without manual pbxproj edits.
class InferenceTests: XCTestCase {}
